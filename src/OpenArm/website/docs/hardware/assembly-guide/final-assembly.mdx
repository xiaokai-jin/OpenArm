---
title: 8. Final assembly
sidebar_position: 10
description: ""
---

import AssemblyGuideImage from '@site/src/components/AssemblyGuideImage';

# Step 8: Final assembly

1. Attach `J2-J3 Sub-Assembly` to the rotor of `J3` motor using 6 `M3x15` bolts

<AssemblyGuideImage src="final-assembly/step1.png" alt="step1" width="30%" />

2. Attach `J2_A` to the rotor of `J2` motor using 6 `M5x10` bolts.

<AssemblyGuideImage src="final-assembly/step2.png" alt="step2" width="100%" />

3. Connect the `J8_A` component from the `Gripper Sub-Assembly` to the rotor of `J7` motor using 6 `M3x15` bolts.

<AssemblyGuideImage src="final-assembly/step3.png" alt="step3" width="30%" />

:::tip
When assembling the leader arms, attach the `J8_A` component from the `Leader End-Effector Sub-Assembly`.
:::

4. Finally, mount the arms on the pedestal using 4 `M6x12` bolts in `J1_A`. Make sure you have already inserted T-slot nuts inside the aluminium extrusion. Align the top of `J1_A` to the top of the aluminium extrusion.

:::tip
The Power+CAN ports of the `J1` motor of both the arms face the front. The reinforcement in the pedestal is towards the back.
:::

:::info
<AssemblyGuideImage src="final-assembly/follower-skeleton-render.png" alt="step5" width="50%" />
:::
